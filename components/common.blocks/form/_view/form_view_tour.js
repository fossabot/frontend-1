modules.define( 'form', function( provide, Form ) {

provide( Form.declMod( { modName: 'view', modVal: 'tour' }, {
    onSetMod: {
        js: {
            inited: function() {
                
            }
        }
    }
} ) );

} );
