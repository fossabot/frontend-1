modules.define( 'tour__time', ['i-bem-dom'], function( provide, bemDom ) {

provide( bemDom.declElem( 'tour', 'time', {
    onSetMod: {
        js: {
            inited: function() {
                
            }
        }
    }
} ) );

} );
