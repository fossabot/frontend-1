modules.define( 'tour__date', ['i-bem-dom'], function( provide, bemDom ) {

provide( bemDom.declElem( 'tour', 'date', {
    onSetMod: {
        js: {
            inited: function() {
                
            }
        }
    }
} ) );

} );
